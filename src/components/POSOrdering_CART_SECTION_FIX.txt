/* REPLACE LINES 1789-1795 in POSOrdering.tsx WITH THIS CLEAN CODE: */

            </div>
          )}
        </div>

        <Separator className="shadow-sm" />
        
        <div className="p-3 space-y-2 bg-gradient-to-r from-blue-50 to-blue-100">
          {/* Inline Promo Code Input */}

/* THEN DELETE LINES 1796-1984 (the entire duplicate cart rendering block) */
/* The next line after your edits should be line 1989 showing: */
/*   {!appliedPromoCode && cart.length > 0 && ( */
